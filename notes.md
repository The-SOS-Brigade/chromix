docker container builds the system

`build.sh` is run and it formats the drive in which user wants to install
then it chroots to drive and runs commands from there to use apt and get our dependencies
thats it.

1. Development (catgirls & junaga)
2. Installation (user)

<!-- ???? Chronological flow of system programming, creation, installation and runtime

build
runtime -->

- install: figlet
- study: https://en.wikipedia.org/wiki/User_interface

```bash
git log --branches --graph --date=short --pretty=format:"%C(yellow)%h %C(auto)%ad %C(Blue)%an%Creset %s %C(auto)%d%Creset"
```
